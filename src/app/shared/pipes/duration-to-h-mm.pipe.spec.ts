import { DurationToHMmPipe } from './duration-to-h-mm.pipe';

describe('DurationToHMmPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationToHMmPipe();
    expect(pipe).toBeTruthy();
  });
});
